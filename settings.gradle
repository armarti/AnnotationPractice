pluginManagement {
    plugins {
        id 'java'
        id 'scala'
        id 'idea'
        id "org.jetbrains.kotlin.jvm" version "${kotlinJvmVersion}"
    }
}

rootProject.name = "${projectName}"
